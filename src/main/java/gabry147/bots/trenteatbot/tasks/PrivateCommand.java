package gabry147.bots.trenteatbot.tasks;

//command already generated
//order is important: new command at the end of the list
public enum PrivateCommand {
	APPROVA, LIMITA, BAN, CANCELLAUTENTE, USERS;
}
